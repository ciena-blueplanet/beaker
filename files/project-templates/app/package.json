{
  "name": "{{ projectName }}",
  "version": "0.0.1",
  "main": "src/index.6.js",
  "repository": {
    "type": "git",
    "url": "git@{{ githubHost }}:{{ githubUser }}/{{ projectName }}.git"
  },
  "devDependencies": {
    "beaker": "^{{ beakerVersion }}"
  },
  "dependencies": {
    "jquery": "^2.1.3",
    "lodash": "^3.2.0"
  },
  "publishConfig": {
    "registry": "{{ npmRegistry }}"
  }
}
