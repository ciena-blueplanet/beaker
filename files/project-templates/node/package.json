{
  "name": "{{ projectName }}",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "git@{{ githubHost }}:{{ githubUser }}/{{ projectName }}.git"
  },
  "devDependencies": {
    "beaker": "^{{ beakerVersion }}"
  },
  "dependencies": {
    "lodash": "^3.2.0"
  },
  "publishConfig": {
    "registry": "{{ npmRegistry }}"
  }
}
